Basic Bash script which calls the 'aws cloudformation create-stack' API to execute the CloudFormation stack build.
Parameters are passed in via the command line to ensure the script is repeatable  across different VPCs. Example parameters have been added.
